# Backend .env example
# Database (Docker)
DATABASE_URL=postgres://paneladecasadb:paneladecasapass@postgres:5432/paneladecasa

# Database (Local)
# DATABASE_URL=postgresql://user:password@localhost:5432/paneladecasa

# JWT Secret (troque em produção!)
JWT_SECRET=sua_senha_secreta_super_segura_aqui

# Server Port
PORT=4000

# Node Environment
NODE_ENV=development

# Upload Configuration
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# CORS (separe por vírgula para múltiplas origens)
# Ex: CORS_ORIGIN=http://localhost:3000,https://seu-frontend.vercel.app
CORS_ORIGIN=http://localhost:3000

# Production Variables (configure nos serviços de deploy)
# DATABASE_URL=postgresql://user:pass@host:port/db_prod
# JWT_SECRET=sua_senha_jwt_super_secreta_aqui_production
# NODE_ENV=production
# PORT=8080
# CORS_ORIGIN=https://seu-frontend.vercel.app
# PORT=8080
